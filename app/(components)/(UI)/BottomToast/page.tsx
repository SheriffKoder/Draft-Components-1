import React from 'react'
import BottomToast from './BottomToast'

const page = () => {
  return (
    <div className='flex justify-center items-center h-screen'>
      <BottomToast Cost={100} />
    </div>
  )
}

export default page
